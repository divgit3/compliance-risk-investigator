# ── Data Processing ──────────────────────────────────────────────────────────
# Core data manipulation, file formats, and synthetic data generation
pandas==2.2.2
pyarrow>=14.0.0,<16.0.0
numpy==1.26.4
faker==25.2.0
requests==2.32.3
PyAthena==3.20.0

# ── dbt (Data Build Tool) ────────────────────────────────────────────────────
# SQL-based transformation layer; dbt-duckdb runs locally without a server
dbt-core==1.8.3
dbt-duckdb==1.8.1

# ── Vector Database ──────────────────────────────────────────────────────────
# Qdrant client for storing and querying policy document embeddings
qdrant-client>=1.9.0

# ── PDF Processing ────────────────────────────────────────────────────────────
# PyMuPDF (fitz) for high-fidelity PDF text extraction in embed_policy_docs.py
# PyPDF2 retained for legacy policy_doc_loader.py compatibility
PyPDF2==3.0.1
pymupdf

# ── LangChain (AI Agents — Phase 3) ─────────────────────────────────────────
# Phase 3 agent framework: ReAct agents, tool calling, OpenAI integration
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langsmith>=0.1.0
langchainhub>=0.1.21

# ── OpenAI ───────────────────────────────────────────────────────────────────
# Direct OpenAI SDK (used by langchain-openai, policy tools, and standalone calls)
openai>=1.40.0

# ── API Layer ────────────────────────────────────────────────────────────────
# FastAPI serves the backend; uvicorn is the ASGI server; pydantic validates data
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# ── Streamlit Dashboard (Phase 4) ────────────────────────────────────────────
# Streamlit dashboard, interactive charts, 3D maps, and network graph
streamlit>=1.36.0
plotly>=5.22.0
pydeck>=0.9.0
pyvis>=0.3.2
httpx>=0.27.0
watchdog>=4.0.0

# ── Machine Learning ─────────────────────────────────────────────────────────
# scikit-learn for anomaly detection models; shap for feature explanations (Task 3.7)
# mlflow tracks experiments and model runs
scikit-learn==1.5.0
pyod
shap
mlflow>=2.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
# python-dotenv loads .env files; loguru provides structured logging
python-dotenv==1.0.1
loguru==0.7.2

# ── AWS ──────────────────────────────────────────────────────────────────────
boto3

# ── Testing ──────────────────────────────────────────────────────────────────
# pytest for unit + integration tests; httpx provides async client for API tests
pytest
pytest-asyncio>=0.23.0
awswrangler>=3.14.0

# ── Evaluation ───────────────────────────────────────────────────────────────
ragas>=0.4.0
