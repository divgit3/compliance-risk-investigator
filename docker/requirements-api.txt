fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<2.0.0
pyarrow>=14.0.0
langchain>=0.2.0,<0.3.0
langchain-openai>=0.1.0,<0.2.0
langchain-community>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.3.0
openai>=1.35.0,<2.0.0
qdrant-client>=1.9.0
scikit-learn>=1.4.0
shap>=0.45.0
mlflow>=2.0.0
httpx
python-dotenv

# ── Test dependencies ────────────────────────────────────────────────────────
# Required to run /app/tests/* inside the API container. Tests live in the
# image (immutable build artifact pattern), so the test runner must too.
pytest>=8.0.0,<10.0.0
pytest-asyncio>=0.23.0,<2.0.0